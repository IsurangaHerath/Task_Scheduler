[build]
  command = "cd client && npm ci && npm run build"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (also shipped via client/public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200